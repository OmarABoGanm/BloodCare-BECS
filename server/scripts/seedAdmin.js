require('dotenv').config();const mongoose=require('mongoose');const connect=require('../config/database');const service=require('../services/userService');const {passwordValid}=require('../services/authService');
(async()=>{const {ADMIN_USERNAME,ADMIN_EMAIL,ADMIN_PASSWORD}=process.env;if(!/^[a-zA-Z0-9_.-]{3,80}$/.test(ADMIN_USERNAME||'')||!/^\S+@\S+\.\S+$/.test(ADMIN_EMAIL||'')||!passwordValid(ADMIN_PASSWORD))throw new Error('Set valid ADMIN_USERNAME, ADMIN_EMAIL and ADMIN_PASSWORD in server/.env. Password requires 8–72 characters, uppercase, lowercase and a number.');await connect();const user=await service.seedAdmin({username:ADMIN_USERNAME,email:ADMIN_EMAIL,password:ADMIN_PASSWORD,fullName:ADMIN_USERNAME});console.log(user?'Initial administrator created.':'Active administrator already exists; no changes made.');await mongoose.disconnect();})().catch(async()=>{console.error('Admin initialization failed. Check environment configuration and database connection; credentials were not logged.');await mongoose.disconnect();process.exitCode=1;});
