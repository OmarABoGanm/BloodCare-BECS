const express=require('express'); const helmet=require('helmet'); const cors=require('cors'); const mongoose=require('mongoose'); const {notFound,errorHandler}=require('./middleware/errorHandler');
const app=express(); app.use(helmet()); app.use(cors({origin:process.env.CLIENT_URL||'http://localhost:5173',exposedHeaders:['Content-Disposition']})); app.use(express.json({limit:'50kb'}));
app.get('/api/health',(req,res)=>res.json({success:true,data:{application:'ok',database:mongoose.connection.readyState===1?'connected':'disconnected'}}));
app.use('/api/compatibility',require('./routes/compatibilityRoutes')); app.use('/api/inventory',require('./routes/inventoryRoutes')); app.use('/api/donations',require('./routes/donationRoutes')); app.use('/api/requests',require('./routes/requestRoutes')); app.use('/api/emergency',require('./routes/emergencyRoutes'));app.use('/api/audit',require('./routes/auditRoutes'));app.use('/api/exports',require('./routes/exportRoutes')); app.use(notFound); app.use(errorHandler); module.exports=app;
