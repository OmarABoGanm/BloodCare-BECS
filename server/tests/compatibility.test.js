const s=require('../services/bloodCompatibilityService');
const matrix={'O-':['O-'],'O+':['O+','O-'],'A-':['A-','O-'],'A+':['A+','A-','O+','O-'],'B-':['B-','O-'],'B+':['B+','B-','O+','O-'],'AB-':['AB-','A-','B-','O-'],'AB+':['AB+','AB-','A+','A-','B+','B-','O+','O-']};
describe('authoritative RBC compatibility matrix',()=>{test.each(Object.entries(matrix))('%s receives only compatible donors',(recipient,donors)=>{expect(s.getCompatibleDonors(recipient)).toEqual(donors); for(const donor of Object.keys(matrix)) expect(s.isCompatible(donor,recipient)).toBe(donors.includes(donor));}); test.each(Object.keys(matrix))('%s donor recipients are derived correctly',(donor)=>{expect(s.getCompatibleRecipients(donor)).toEqual(Object.keys(matrix).filter(r=>matrix[r].includes(donor)));}); test('strictly rejects invalid values',()=>expect(()=>s.getCompatibleDonors('A')).toThrow('Invalid blood type'));});
describe('ranking',()=>{const inv=['O-','O+','A-','A+','B-','B+','AB-','AB+'].map(bloodType=>({bloodType,unitsAvailable:20,reservedUnits:0})); test('prefers sufficient exact match',()=>expect(s.rankCompatibleDonors('A+',inv,2)[0].bloodType).toBe('A+')); test('preserves O- when alternatives exist',()=>expect(s.rankCompatibleDonors('A+',inv,2).at(-1).bloodType).toBe('O-')); test('marks zero and insufficient inventory',()=>{const ranked=s.rankCompatibleDonors('A+',[{bloodType:'A+',unitsAvailable:0}],2);expect(ranked[0].sufficient).toBe(false);}); test('is deterministic',()=>expect(s.rankCompatibleDonors('AB+',inv,2)).toEqual(s.rankCompatibleDonors('AB+',inv,2)));});
