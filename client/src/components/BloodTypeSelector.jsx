import { BLOOD_TYPES } from '../utils/constants';
export default function BloodTypeSelector({value,onChange,label='Blood type',name='bloodType'}){return <fieldset className="mb-4"><legend className="form-label fw-semibold">{label}</legend><div className="blood-grid" role="radiogroup" aria-label={label}>{BLOOD_TYPES.map(type=><label className={`blood-option ${value===type?'selected':''}`} key={type}><input className="visually-hidden" type="radio" name={name} value={type} checked={value===type} onChange={e=>onChange(e.target.value)}/><span>{type}</span></label>)}</div></fieldset>}
